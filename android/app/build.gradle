plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.campuseyeapp"
    compileSdk = 34  // Use the latest stable version.

    ndkVersion = "25.2.9519653" // Replace with the NDK version installed in your SDK.

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId = "com.example.campuseyeapp"
        minSdk = 21  // Update based on the Flutter dependencies used in your project.
        targetSdk = 34 // Use the latest stable version.
        versionCode = 1 // Update as needed.
        versionName = "1.0" // Update as needed.
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

